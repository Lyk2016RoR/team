<h1> <%= @category.name%></h1>

<% @category.books.each do |book|%>
    <b>ID </b><%= book.id %><br>
    <b>Title </b>
    <%= link_to book.name, book_path(book) %><br>
    <b>Description </b><%= book.description %><br><hr>
<% end %>

<%=link_to "Go Back to books", books_path%>